# R5 Visual QA Report

Review source: the final PNGs generated by
`scripts/test_r5_unified_workbench.cjs`, inspected through
`evidence/r5/CONTACT_SHEET.png` and the original-resolution screenshots.

| Image | Observed visually | Potential issue | Expected? | Action / decision |
|---|---|---|---|---|
| 01 overview | Hero, project KPIs, Draft badge and one/one topology are readable in the first viewport. | Dark UI makes small metadata subdued. | Yes for a dense engineering shell. | Keep; hierarchy is clear. |
| 02 BOM | Three Siemens rows, quantities and candidate status are aligned in one table. | Long order-code text is dense. | Yes; codes are technical identifiers. | Keep; no clipping or overlap. |
| 03 panel loaded | Plate, two rails, three ducts and three outlined device projections are visible. | Projection rectangles are not native CAD geometry. | Yes; they are explicitly panel projections. | Keep; inspector and legend provide context. |
| 04 selected device | HMI selection is visible and the truth inspector exposes identity, footprint, mounting and CAD link. | Right column is information-dense. | Yes for a review workflow. | Keep; key fields remain readable. |
| 05 candidate drag | HMI rectangle visibly moves while Draft/candidate status remains in the shell. | The 55 mm drag is subtle at full-cabinet scale. | Acceptable because the inspector and before/after state establish the change. | Keep; test records the numeric delta. |
| 06 locked reflow | HMI remains fixed and other projections stay arranged around the locked anchor. | Red outlines can look like warnings. | Expected selection/lock affordance. | Keep; lock action is visible in inspector. |
| 07 KTP700 CAD | White source drawing, linked HMI title and populated CAD inspector are visible; geometry is non-blank. | KTP drawing has generous whitespace at fit scale. | Expected for the source sheet aspect ratio. | Keep; fit view is honest and not cropped. |
| 08 G120C CAD | G120C source geometry is centered and readable with linked VFD1 context. | Text annotations are small at overview zoom. | Expected for a full source drawing. | Keep; deep zoom control is available. |
| 09 CAD selection/layer | Cyan bounds, selected entity state and populated layer list are visible beside the drawing. | Debug metadata is verbose. | Required for the evidence gate. | Keep; it proves the bridge state. |
| 10 panel-to-CAD | Same G120C drawing remains visible with VFD1 title after navigation. | Similar composition to image 09. | Expected; this shot proves retained context, not a new geometry state. | Keep; metadata differentiates the action. |
| 11 validation | Validation issues and review gate are separated into readable panels. | Candidate issues occupy a compact list. | Expected in a scan-oriented tool. | Keep; no overlay obscures content. |
| 12 export | Export controls and JSON audit output are visible; `valid` result is in the viewport. | JSON is dense and monospaced. | Required for an audit artifact. | Keep; output is intentionally inspectable. |
| 13 invalid DXF | Error text says last valid scene retained, input guard explains the 9-byte rejection, and G120C geometry remains rendered (546). | Source drawing is smaller than the canvas. | Expected after restoring the last valid scene at fit. | Keep; this is the corrected post-race evidence. |

## Gate result

No screenshot is blank, cropped, or blocked by a warning overlay. The initial
version of image 13 captured a transient restore spinner; the test was fixed to
avoid overlapping asset opens and to wait for a stable rendered scene. The
contact sheet and all 13 PNGs were regenerated after that fix.
